# scale_images

Scales images in a directory to a target width (default 680px) while keeping the aspect ratio.

Usage:

```bash
python scale_images.py --dir /path/to/images --width 680 --suffix _scaled --recursive
```

Dependencies:

- Pillow (see `requirements.txt`)
